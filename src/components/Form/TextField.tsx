import { TextField as TextFieldComponent } from "@dotkomonline/design-system";
import styled from "styled-components";

const TextField = styled(TextFieldComponent)`
  margin-bottom: 10px;
  width: 80%;
`;

export default TextField;
